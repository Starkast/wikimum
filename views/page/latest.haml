%h1 Senast ändrad
- @page_groups.each do |date, pages|
  %h2= date
  %ul
    - pages.each do |page|
      %li
        %a{ href: "#{page.slug}" }= page.title
        %span.description= page.description
